{
  "name": "jazzene",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "moon check && moon fmt --check",
    "format": "moon fmt",
    "test": "moon test -v -p internal/jazzene/core --target wasm-gc",
    "test:e2e": "playwright test",
    "dev": "vite",
    "build": "moon build && vite build"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@tailwindcss/vite": "^4.1.18",
    "tailwindcss": "^4.0.2",
    "vite": "^6.0.0",
    "vite-plugin-moonbit": "^0.1.0"
  }
}
